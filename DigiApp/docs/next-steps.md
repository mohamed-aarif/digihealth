Next steps to production:
- Hook up real identity API (ABP) and implement OAuth2/OIDC flows.
- Add secure token storage (expo-secure-store / Keychain) in mobile.
- Configure EAS with store credentials and Vercel for web.
- Add end-to-end tests (Detox) and unit tests (Jest).
