name: Infra (dev)

on:
  workflow_dispatch:            # 👈 allows manual trigger from Actions tab
  push:
    branches: [ "main" ]
    paths:
      - "infra/terraform-azure/**"
  pull_request:
    paths:
      - "infra/terraform-azure/**"

permissions:
  id-token: write
  contents: read

env:
  TF_WORKDIR: infra/terraform-azure/envs/dev
  ARM_USE_OIDC: true
  ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
  ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
  TF_VAR_tenant_id: ${{ vars.TF_VAR_tenant_id }}
  # If you use key-based backend instead of AAD role for tfstate:
  ARM_ACCESS_KEY: ${{ secrets.ARM_ACCESS_KEY }}

jobs:
  terraform:
    name: Terraform Dev
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.5

      - name: Terraform Init
        working-directory: ${{ env.TF_WORKDIR }}
        # If you keep backend.hcl next to providers.tf, uncomment:
        # run: terraform init -input=false -backend-config=backend.hcl
        run: terraform init -input=false

      - name: Terraform Validate
        working-directory: ${{ env.TF_WORKDIR }}
        run: terraform validate

      - name: Terraform Plan (PRs)
        if: github.event_name == 'pull_request'
        working-directory: ${{ env.TF_WORKDIR }}
        run: terraform plan -input=false

      - name: Terraform Apply (main)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        working-directory: ${{ env.TF_WORKDIR }}
        run: terraform apply -auto-approve
