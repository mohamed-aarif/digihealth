{
  "name": "digihealth-frontend",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:mobile": "pnpm --filter @digihealth/mobile start",
    "dev:web": "pnpm --filter @digihealth/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  }
}